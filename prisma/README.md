# Prisma Directory

This directory is reserved for the database layer.

Phase 3 will add:

- `schema.prisma`
- migrations
- database seed utilities if needed later

The initial schema is designed for:

- Supabase Auth as the identity source
- Prisma-managed application tables in `public`
- auditable rentals, sessions, usage, and billing flows
